import seeFetch from 'see-fetch';

const req = {
  // id
};

seeFetch.config('promo/ci/dynamic/delete', {
  method: ['post'],
  stringify: [!0],
  url: [
    '/',
    '/promo/ci/dynamic/mock/success',
    '/promo/ci/dynamic/mock/success',
  ],
  req: [req, req],
});
