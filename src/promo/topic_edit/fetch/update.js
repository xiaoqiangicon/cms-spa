import seeFetch from 'see-fetch';

const req = {};

seeFetch.config('promo/statistics/updateManagerList', {
  method: ['post'],
  stringify: [!0],
  url: ['//', '/promo/topicEdit/mock/success', '/promo/topicEdit/mock/success'],
  req: [req, req],
});
